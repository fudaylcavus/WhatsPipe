enum MessageStatus
{
    RECEIVED,
    SENT,
    SYSTEM
};


enum MessagePart
{
    FROM,
    CONTENT,
    TARGET
};

enum BubbleRow 
{
    TOP,
    MIDDLE,
    BOTTOM
};

enum Aligment
{
    LEFT,
    RIGHT
};